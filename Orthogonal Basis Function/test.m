% Remark: use MATLAB to solve the exercises. 
% Write your own code for computing the coefficients. 
% Make sure to comment on your resulting approximations and try to draw conclusions
% when possible.

function test = is_even(x)
% is_even returns 1 if x is even
y = ~mod(x,2);
end
